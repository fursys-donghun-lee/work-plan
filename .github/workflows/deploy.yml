name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Check Firebase secrets (lengths only — values not exposed)
        env:
          K1: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
          K2: ${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          K3: ${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          K4: ${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          K5: ${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          K6: ${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
        run: |
          echo "API_KEY length: ${#K1}"
          echo "AUTH_DOMAIN length: ${#K2}"
          echo "PROJECT_ID length: ${#K3}"
          echo "STORAGE_BUCKET length: ${#K4}"
          echo "MESSAGING_SENDER_ID length: ${#K5}"
          echo "APP_ID length: ${#K6}"

      - name: Build (static export)
        env:
          # 사용자 사이트(<user>.github.io)가 아니면 저장소 이름을 basePath 로 주입
          # 저장소 이름이 자동으로 GITHUB_REPOSITORY 의 뒷부분
          NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }}
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
        run: npm run build

      - name: Add .nojekyll
        run: touch out/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
